@using Microsoft.AspNetCore.Http

<nav class="navbar navbar-expand-lg bg-body-tertiary mb-3">
    <div class="container">
        <a class="navbar-brand" asp-controller="Home" asp-action="Index">🎵 The Vinyl Countdown</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Home" asp-action="Index">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Albums" asp-action="Index">All Albums</a>
                </li>
                @if (Context.Session.GetInt32("userId") != null)
                {
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Albums" asp-action="New">Add Album</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Albums" asp-action="MyAlbums">My Albums</a>
                    </li>
                }
            </ul>
            <ul class="navbar-nav ms-auto me-3">
                @if (Context.Session.GetInt32("userId") != null)
                {
                    <li class="nav-item">
                        <span class="navbar-text me-3">
                            Welcome, @Context.Session.GetString("username")!
                        </span>
                    </li>
                }
            </ul>
        </div>
        @if (Context.Session.GetInt32("userId") is not null)
        {
            <a class="btn btn-sm btn-primary" asp-controller="Account" asp-action="ConfirmLogout">Log out</a>
        }
        else
        {
            <a class="btn btn-sm btn-primary me-2" asp-controller="Account" asp-action="RegisterForm">Register</a>
            <a class="btn btn-sm btn-primary" asp-controller="Account" asp-action="LoginForm">Login</a>
        }
    </div>
</nav>
